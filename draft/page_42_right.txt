📄 الصورة: page_42_right.jpg
🕒 التاريخ: 2025-07-31 00:07:40.477136

🧠 النص المستخرج:


🧮 LaTeX:
بالتأكيد، إليك المعادلات والتعابير الرياضية محولة إلى LaTeX:

1.  **س = 5**
    ```latex
    x = 5
    ```

2.  **2ص + 3 = 7**
    ```latex
    2y + 3 = 7
    ```

3.  **أ < ب**
    ```latex
    a < b
    ```

4.  **ج > د**
    ```latex
    c > d
    ```

5.  **هـ ≥ و**
    ```latex
    e \geq w
    ```

6.  **ز ≤ ح**
    ```latex
    z \leq h
    ```

7.  **ك ≠ ل**
    ```latex
    k \neq l
    ```

8.  **م^2**
    ```latex
    m^2
    ```

9.  **ن^3**
    ```latex
    n^3
    ```

10. **ص = س^2 + 2س - 1**
    ```latex
    y = x^2 + 2x - 1
    ```

11. **ع = جذر(ب)**
    ```latex
    c = \sqrt{b}
    ```

12. **د = جذر(49)**
    ```latex
    d = \sqrt{49}
    ```

13. **هـ = جذر(س^2 + ص^2)**
    ```latex
    e = \sqrt{x^2 + y^2}
    ```

14. **3 / 4**
    ```latex
    \frac{3}{4}
    ```

15. **(أ + ب) / ج**
    ```latex
    \frac{a + b}{c}
    ```

16. **س = (-ب ± جذر(ب^2 - 4أج)) / 2أ** (الصيغة التربيعية)
    ```latex
    x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
    ```

**ملاحظة:** لكي تظهر هذه المعادلات بشكل صحيح في مستند LaTeX، يجب أن تكون محاطة ببيئة رياضية. على سبيل المثال، يمكنك استخدام `$` للمتغيرات والمعادلات الصغيرة داخل النص، أو `\[...\]` (أو `$$...$$` لمزيد من التحكم في العرض) لعرض المعادلات الكبيرة في سطر منفصل:

*   **مثال (داخل النص):** `القيمة هي $x = 5$.`
*   **مثال (سطر منفصل):**
    ```latex
    \[
    x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
    \]
    ```