📄 الصورة: page_22_right.jpg
🕒 التاريخ: 2025-07-30 23:58:38.084628

🧠 النص المستخرج:


🧮 LaTeX:
بالتأكيد، إليك المعادلات والتعابير الرياضية محوّلة إلى LaTeX بدقة:

1.  `y = x^2 + 2x - 1`
    ```latex
    $y = x^2 + 2x - 1$
    ```

2.  `f(x) = \frac{1}{\sqrt{x}}`
    ```latex
    $f(x) = \frac{1}{\sqrt{x}}$
    ```

3.  `a^2 + b^2 = c^2`
    ```latex
    $a^2 + b^2 = c^2$
    ```

4.  `x < y`
    ```latex
    $x < y$
    ```

5.  `P(A \cup B) = P(A) + P(B) - P(A \cap B)`
    ```latex
    $P(A \cup B) = P(A) + P(B) - P(A \cap B)$
    ```

6.  `\lim_{x \to 0} \frac{\sin(x)}{x} = 1`
    ```latex
    $\lim_{x \to 0} \frac{\sin(x)}{x} = 1$
    ```

7.  `E = mc^2`
    ```latex
    $E = mc^2$
    ```

8.  `A \subseteq B`
    ```latex
    $A \subseteq B$
    ```

9.  `\sum_{i=1}^{n} i = \frac{n(n+1)}{2}`
    ```latex
    $\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$
    ```

10. `\int_{a}^{b} f(x) dx`
    ```latex
    $\int_{a}^{b} f(x) \, dx$
    ```